{{ define "sql_script_table" }}# auto generated by tuk-gen
;

{{ range $table := . -}}
CREATE TABLE IF NOT EXISTS {{ .TableIdentifier }} (
	{{ $lines := $table.DefLines -}}
	{{ $maxIdx := $lines.MaxIdx -}}
	{{ range $idx, $line := $lines -}}
	{{ $line }}
	{{- if ne $idx $maxIdx }}, 
	{{ end -}}
	{{ end }}
)
{{ $extraLines := $table.ExtraLines -}}
{{ range $line := $extraLines -}}
{{ $line }}
{{ end -}}
;

{{ end }}
{{ end }}
